server:
  port: 8080

spring:
  application:
    name: loan-risk-backend
  datasource:
    url: jdbc:mysql://localhost:3306/loan_risk_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Ijse@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# FastAPI ML Microservice URL
ml-service:
  base-url: http://localhost:8000