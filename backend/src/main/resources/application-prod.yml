spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql-db}:3306/${DB_NAME:loan_risk_db}?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:Sachintha@2003}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

ml-service:
  base-url: ${ML_SERVICE_URL:http://ml-service:8000}